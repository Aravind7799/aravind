<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Surprise Message 🎁</title>
<meta name="viewport" content="width=device-width, initial-scale=1">

<style>
body{
  margin:0;
  font-family: Arial, sans-serif;
  background: linear-gradient(135deg,#ff9a9e,#fad0c4);
  min-height:100vh;
  display:flex;
  justify-content:center;
  align-items:center;
}
.box{
  background:#fff;
  width:90%;
  max-width:400px;
  padding:20px;
  border-radius:15px;
  text-align:center;
  box-shadow:0 10px 30px rgba(0,0,0,.2);
}
input,textarea{
  width:100%;
  padding:10px;
  margin:8px 0;
  border-radius:8px;
  border:1px solid #ccc;
}
textarea{height:80px}
button{
  background:#ff4d6d;
  color:#fff;
  border:none;
  padding:10px 20px;
  border-radius:20px;
  cursor:pointer;
}
.card{
  display:none;
  margin-top:15px;
  background:#fff0f5;
  padding:15px;
  border-radius:10px;
}
</style>
</head>

<body>

<div class="box">
  <h2>🎁 Surprise Message</h2>

  <div id="form">
    <input id="from" placeholder="From">
    <input id="to" placeholder="To">
    <textarea id="msg" placeholder="Your message"></textarea>
    <button onclick="make()">Create Surprise</button>
  </div>

  <div class="card" id="card">
    <h3 id="toText"></h3>
    <p id="msgText"></p>
    <b>— <span id="fromText"></span></b><br><br>
    <button onclick="copy()">Copy Link 🔗</button>
    <p id="link" style="font-size:12px;"></p>
  </div>
</div>

<script>
function make(){
  let f=from.value,t=to.value,m=msg.value;
  if(!f||!t||!m){alert("Fill all fields");return;}
  let link=location.origin+location.pathname+
  `?f=${encodeURIComponent(f)}&t=${encodeURIComponent(t)}&m=${encodeURIComponent(m)}`;
  show(f,t,m,link);
}
function show(f,t,m,l){
  form.style.display="none";
  card.style.display="block";
  toText.innerText="Dear "+t;
  msgText.innerText=m;
  fromText.innerText=f;
  link.innerText=l;
}
function copy(){
  navigator.cl
